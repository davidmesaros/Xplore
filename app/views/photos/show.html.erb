	<div class ="navbar navbar-fixed-top">
    <div class ="navbar-inner">
      <div class ="container">
        <div class = "nav">
					
					<h1 >Hello! <%= link_to(@photo.user.username, @photo.user) if @photo.user %>
						<%= image_tag(@photo.user.avatar_url, :class => 'userlogin') if @photo.user %>
					<h1>
					</div>
	    </div>
	  </div>
	</div>

		
		<div class="container">

        <div class="row">
            <div class="box">
               <div class="col-lg-12 text-center">
                  <div class="img-responsive img-border img-full">
										<h3> New Photo Added </h3>
										<%= image_tag(@photo.image_url, :class => 'xthumb') if @photo.user %>
										
										<hr>
                  
                 
                </div>
             </div>
          </div>
      </div>
							        
<footer>
   <div class ="navbar navbar-fixed-bottom">
    <div class ="navbar-inner">
      <div class ="container">
        <div class = "nav1"	>		


							<% if @photo.user == @current_user %>

									<%= link_to'Edit PHoto! |', edit_photo_path %>
									<%= link_to(' Add  A New Photo |', new_photo_path) %>
									<%= link_to'Delete photo |', @photo, :data => {:method => :delete, :confirm => 'Really delete this PHOto?' } %>

									
							<% end %>

									<%= link_to " check out Xplore feed", photos_path %> 
									<p><%= link_to('Back to search', root_path) %></p>
						</div>      
        </div>
      </div>   
    </div>
  </footer>

	 

  